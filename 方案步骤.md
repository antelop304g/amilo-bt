# 有线键盘转蓝牙：ESP32-S3 方案步骤

把现有的 USB 键盘变成蓝牙键盘。电脑在蓝牙里配对后直接打字，不用接收器，也不用装驱动。

这次只做键盘，不做鼠标。键盘是阿米洛熊猫 87 键（VA87M），普通有线 USB 键盘，可以用。

预算大约 20–30 元。不打板，不焊接芯片。

## 做出来是什么样

```
手机充电器 ──红/黑线──► ESP32-S3 SuperMini 的 5V / GND
                              │
                         Type-C
                              │
                           OTG 头
                              │
                    阿米洛熊猫（USB-A 那头）
                              │
                           蓝牙
                              │
                            电脑
```

Type-C 不能同时插电脑和键盘。烧程序时插电脑；用的时候插键盘，电从 5V 脚进来。

## 1. 购买

在淘宝按下面三句搜，别买成别的型号。

| 搜什么 | 要确认 | 不要买 |
| --- | --- | --- |
| `ESP32-S3 SuperMini` | 标题里有 **S3**，板上有 **5V**、**GND**、**3V3** 丝印 | C3、普通 ESP32、ESP32-S2。外观很像，没有可用的 USB 主机 |
| `Type-C 公头转 USB 母口 OTG` | 写明能传数据 | 只充电的线 |
| `USB 公头转杜邦线` | 只要红线和黑线 | 自己剪充电线 |

充电器用家里现成的 5V 手机充电器。

阿米洛熊猫的线，电脑那头必须是 **USB-A**，才能插进 OTG 头。自带线如果是 Mini-USB 或 Type-C 转 USB-A，直接用。如果只有两头都是 Type-C 的线，再买一根转 USB-A 的数据线。

## 2. 到货后认板子

SuperMini 很小。一面是 Type-C，旁边两个小按键，丝印一般是：

- **B**：BOOT，烧录时用
- **R**：复位

排针上找到 **5V**、**GND**、**3V3**。充电器只接 5V 和 GND。**不要把充电器接到 3V3。**

## 3. 装软件，确认板子是活的

这一步先不要接键盘，也不要接充电器。

1. 安装 [Arduino IDE 2](https://www.arduino.cc/en/software)。
2. 开发板管理器里安装 Espressif 的 **esp32** 板级包。
3. 开发板选 **ESP32S3 Dev Module**。
4. Type-C 直接插电脑。
5. 如果端口不出现：按住 **B** 不放，点一下 **R**，再松开 **B**。
6. 打开串口打印示例，烧进去。串口监视器里能看到输出，说明板子、线和软件都通了。

## 4. 接线

Type-C 从电脑上拔掉。以后这个口只接键盘。

1. 杜邦线红线接板子 **5V**，黑线接 **GND**。
2. USB 公头插上手机充电器。
3. 板子 Type-C 插 OTG 头。
4. 键盘线的 USB-A 插进 OTG 的母口。
5. 把阿米洛的背光调到最低，或先关掉。白光比全键 RGB 省电，但第一次仍建议关灯，确认供电正常后再开。

键盘灯或指示灯亮，说明电到了。不亮就马上拔电。常见原因：

- 5V 和 GND 接反
- 这块板的 5V 脚没有接到 Type-C 的电源上。这种板换一块，不要硬改

## 5. 烧键盘程序

程序在 `keyboard_bridge/keyboard_bridge.ino`。它做两件事：用 USB 读键盘按键，再把这块板伪装成名叫 **Amilo-BT** 的蓝牙键盘。没有商店固件，用 Arduino IDE 烧这个工程。

开发板包用 Espressif 的 esp32（已验证 3.3.12），要新到示例里能看到 `USBHostKeyboard`。太旧的包没有 USB 主机接口，编译会报错。

**ESP32 BLE Keyboard**（作者 T-vK）库管理器里搜不到，原版也不能直接配 3.x 核心。从 https://github.com/T-vK/ESP32-BLE-Keyboard 下载 zip，解压成 `~/Documents/Arduino/libraries/ESP32_BLE_Keyboard`，再打补丁：

```bash
cd ~/Documents/Arduino/libraries/ESP32_BLE_Keyboard
patch -p1 < /path/to/amilo-bt/patches/ESP32-BLE-Keyboard-core3.patch
```

补丁只改 `BleKeyboard.cpp`：

- `std::string` 转成 `String` 传给 `BLEDevice::init` 和 `manufacturer()->setValue`，否则编译不过。
- S3 在 3.x 核心里用 NimBLE，没有 0x2902 描述符。原版 `onConnect` 取到空指针，电脑一连就崩溃重启，电脑提示"请尝试重新连接设备"。现在只在 Bluedroid 下设置，并判空。
- 配对改成 `setCapability(ESP_IO_CAP_NONE)` 加 `setAuthenticationMode(true, false, true)`，不用输密码。
- 断开后用 `BLEDevice::startAdvertising()` 重新广播，扫描响应打开。

Apple 芯片的 Mac 要先装 Rosetta（`softwareupdate --install-rosetta`），否则编译报 `ctags: bad CPU type`。

开发板设置：

- Board：ESP32S3 Dev Module
- USB Mode：USB-OTG (TinyUSB)
- USB CDC On Boot：Disabled
- Flash Size：4MB
- PSRAM：Disabled
- Partition Scheme：Huge APP
- CPU Frequency：80MHz（默认 240MHz 板子会烫到七八十度）

USB-OTG 和 USB CDC On Boot 不要同时打开，两个功能抢同一个口。

烧录时：

1. 拔掉键盘和充电器。
2. Type-C 重新插回电脑。端口不出现就按住 **B**，点一下 **R**，再松开 **B**。
3. 打开 `keyboard_bridge.ino`，上传。
4. 烧完再改回「充电器 + OTG + 键盘」的接法。

改程序时重复上面四步。同一个口不能一边烧程序一边当键盘口。

## 6. 配对

1. 按第 4 步接好并上电。
2. Windows：设置 → 蓝牙和其他设备 → 添加设备 → 蓝牙。
3. 选 **Amilo-BT**，配对。
4. 打开记事本打几个字。打得出来，这套就做成了。

## 这把键盘能做什么

阿米洛熊猫 87 键是标准 USB 键盘，字母、数字、修饰键可以转发。

Fn 加音量、播放这些多媒体键，简单转发不一定带得过去。缺了不影响正常打字。

进 BIOS、游戏高回报率，不要指望。这是蓝牙键盘，不是有线键盘。

## 不要做的事

- 不要买 9.9 元的「USB 蓝牙适配器」。那是插在电脑上连耳机的，方向反了。
- 不要买 C3 或普通 ESP32 来替 S3。
- 不要把充电器接到 3V3。
- 不要在 Type-C 还插着电脑时，再从 5V 脚灌电。
